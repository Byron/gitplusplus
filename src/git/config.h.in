#ifndef GIT_CONFIG_H
#define GIT_CONFIG_H

#include <stdint.h>

#define GIT_HEADER_BEGIN
#define GIT_HEADER_END

// have to implement this ourselves, would only need the unique_file function
// which we can do ourselves
// #define BOOST_FILESYSTEM_VERSION 3

/** \namespace git
  * \brief Contains all git related types
  */
#define GIT_NAMESPACE_BEGIN namespace git {
#define GIT_NAMESPACE_END }

// Types
typedef uint64_t uint64;
typedef uint32_t uint32;
typedef uint8_t uchar;

#endif // GIT_CONFIG_H
